#include <ctype.h>
#include <stdio.h>
/**
 * main - alphabet in lowercase
 *
 * Description: Playing play the alphabet game
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char n;

	for (n = 97; n <= 122; n++)
	{
		putchar(n);
		return (0);
	}
}
